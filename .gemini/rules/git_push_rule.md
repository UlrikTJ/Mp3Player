# Git Push Permission Rule

CRITICAL DIRECTIVE:
NEVER execute `git push` or push code to GitHub automatically.

- Always ask for and wait for explicit permission from the USER before running any `git push` command.
- Committing code locally with `git commit` after building and verifying changes is allowed, but pushing to remote repositories (`git push`) requires explicit user confirmation every time.
